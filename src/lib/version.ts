// Single source of truth for the user-visible version string. package.json
// is the canonical version; the release workflow rewrites this file and the
// three build-manifest versions so they stay in sync.
export const VERSION = "0.1.0";
